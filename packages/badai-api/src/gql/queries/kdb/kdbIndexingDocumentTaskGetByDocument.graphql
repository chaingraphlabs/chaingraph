query kdbIndexingDocumentTaskGetByDocument(
    $session: Session!,
    $document_id: [DocumentID!]!,
) {
    kdbIndexingDocumentTaskGetByDocument(
        session: $session,
        document_id: $document_id,
    ) {
        ...IndexingDocumentTaskFields
    }
}
