query KDBGetPagesByDocument($session: Session!, $document_id: DocumentID!, $filters: GetPagesByDocumentFilters = {}) {
  kdbGetPagesByDocument(session: $session, document_id: $document_id, filters: $filters) {
    page_id
    content
    description
    document_id
    task_id
    number
  }
}
