query KDBIndexingDocumentTaskGet(
    $session: Session!,
    $taskID: TaskID!,
) {
    kdbIndexingDocumentTaskGet(
        session: $session,
        taskID: $taskID,
    ) {
        ...IndexingDocumentTaskFields
    }
}